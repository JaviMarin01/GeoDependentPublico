import { ContrasenaValidacion } from './contrasena-validacion';

describe('ContrasenaValidacion', () => {
  it('should create an instance', () => {
    expect(new ContrasenaValidacion()).toBeTruthy();
  });
});
